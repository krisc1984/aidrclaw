---
phase: 06-agent-domain
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - src/main/react/components/virtual-agent/VirtualAgentInterface.tsx
  - src/main/react/components/virtual-agent/VideoPlayer.tsx
  - src/main/react/components/virtual-agent/ChatBubble.tsx
  - src/main/react/components/virtual-agent/InputArea.tsx
  - src/main/react/components/virtual-agent/ProgressBar.tsx
  - src/main/react/components/virtual-agent/ExceptionAlert.tsx
  - src/main/react/hooks/useVirtualAgent.ts
  - src/main/react/store/agentStore.ts
autonomous: false
requirements:
  - AGENT-01
  - AGENT-02
user_setup: []

must_haves:
  truths:
    - "虚拟坐席可通过文本和语音与客户交互"
    - "客户可通过语音按钮或文本输入回复"
    - "界面显示流程进度和当前环节"
    - "异常情况 (遮挡、离席、噪音) 主动提示客户"
  artifacts:
    - path: "src/main/react/components/virtual-agent/VirtualAgentInterface.tsx"
      provides: "虚拟坐席主界面组件"
      exports: ["VirtualAgentInterface (default)"]
    - path: "src/main/react/hooks/useVirtualAgent.ts"
      provides: "虚拟坐席状态管理和 API 调用"
      exports: ["useVirtualAgent"]
    - path: "src/main/react/store/agentStore.ts"
      provides: "Zustand 状态存储"
      exports: ["useAgentStore"]
  key_links:
    - from: "VirtualAgentInterface"
      to: "/api/agent/turn"
      via: "fetch API call in useVirtualAgent"
      pattern: "fetch.*\\/api\\/agent\\/turn"
    - from: "useVirtualAgent"
      to: "MediaRecorder API"
      via: "audio recording"
      pattern: "new MediaRecorder|recorder\\.start"
    - from: "VirtualAgentInterface"
      to: "WebSocket"
      via: "real-time exception alerts"
      pattern: "new WebSocket|ws:\\/\\/"
---

<objective>
实现虚拟坐席对话界面，提供视频、语音、文本交互能力。

Purpose: 为客户提供友好的虚拟坐席交互界面，支持语音和文本输入，实时显示流程进度和异常提示
Output: React 虚拟坐席界面组件、状态管理、API 集成
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-agent-domain/06-RESEARCH.md
@.planning/phases/06-agent-domain/06-CONTEXT.md

# 依赖 Phase 2 的采集插件
@src/main/react/components/recording/WebcamRecorder.tsx
@src/main/react/hooks/useMediaDevices.ts

# 依赖 Plan 01/02 的 Agent 服务
@.planning/phases/06-agent-domain/06-01-PLAN.md
@.planning/phases/06-agent-domain/06-02-PLAN.md
</context>

<interfaces>
<!-- 关键类型和契约 -- 执行器直接使用这些接口 -->

后端 API (Plan 01 创建):
```typescript
// Agent API 请求
interface AgentTurnRequest {
  sessionId: string;
  input: {
    type: 'TEXT' | 'AUDIO';
    text?: string;
    audioData?: string; // Base64
  };
}

// Agent API 响应
interface AgentTurnResponse {
  text: string;
  audioUrl?: string; // Base64 或 URL
  nextNodeId: string;
  metadata: {
    intent: string;
    confidence: number;
  };
}
```

状态管理 (本计划创建):
```typescript
// Agent 状态
interface AgentState {
  sessionId: string | null;
  currentNode: DialogNode | null;
  isRecording: boolean;
  isProcessing: boolean;
  exception: ExceptionAlert | null;
  history: ChatMessage[];
}

// 对话消息
interface ChatMessage {
  id: string;
  role: 'agent' | 'customer';
  text: string;
  audioUrl?: string;
  timestamp: Date;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建虚拟坐席状态管理和 API 集成</name>
  <files>
    src/main/react/store/agentStore.ts
    src/main/react/hooks/useVirtualAgent.ts
    src/main/react/types/agent.ts
    src/main/react/api/agentApi.ts
  </files>
  <action>
    创建虚拟坐席的状态管理和 API 调用逻辑：

    1. **类型定义** (src/main/react/types/agent.ts):
       - DialogNode 类型：id, type, content, question, action, children
       - AgentState 类型：sessionId, currentNode, isRecording, isProcessing, exception, history
       - ChatMessage 类型：id, role, text, audioUrl, timestamp
       - ExceptionAlert 类型：type, message, severity, timestamp

    2. **Zustand Store** (src/main/react/store/agentStore.ts):
       - 创建 useAgentStore hook
       - State: AgentState
       - Actions:
         - setSessionId(id: string): void
         - setCurrentNode(node: DialogNode): void
         - addMessage(message: ChatMessage): void
         - setRecording(recording: boolean): void
         - setProcessing(processing: boolean): void
         - setException(exception: ExceptionAlert | null): void
         - clearHistory(): void

    3. **API 调用** (src/main/react/api/agentApi.ts):
       - createSession(productId: string): Promise<{sessionId: string}>
       - sendTurn(sessionId: string, input: UserInput): Promise<AgentTurnResponse>
       - getDialogTree(treeId: string): Promise<DialogTree>
       - 使用 fetch API，处理错误和重试
       - 音频数据使用 Base64 编码

    4. **虚拟坐席 Hook** (src/main/react/hooks/useVirtualAgent.ts):
       - 注入 useAgentStore
       - 实现 sendTextMessage(text: string): Promise<void>
       - 实现 sendAudioMessage(audioBlob: Blob): Promise<void>
       - 实现 startRecording(): void - 开始录音
       - 实现 stopRecording(): void - 停止录音并发送
       - 实现 handleException(alert: ExceptionAlert): void - 处理异常提示
       - 使用 MediaRecorder API 录制音频

    5. **WebSocket 连接** (用于实时异常提示):
       - connectExceptionWebSocket(sessionId: string): WebSocket
       - 监听 exception 消息类型
       - 收到异常时调用 store.setException
       - 支持断线重连

    6. **错误处理**:
       - API 调用失败时显示 toast 提示
       - 录音权限被拒绝时提示客户
       - 网络断开时提示客户检查网络
  </action>
  <behavior>
    - Test 1: sendTextMessage 调用 API 并更新 store 历史
    - Test 2: sendAudioMessage 将 Blob 转为 Base64 并调用 API
    - Test 3: startRecording 调用 MediaRecorder.start()
    - Test 4: stopRecording 停止录音并调用 sendAudioMessage
    - Test 5: WebSocket 收到 exception 消息时更新 store
    - Test 6: API 调用失败时显示错误提示
  </behavior>
  <verify>
    <automated>npm test -- --testPathPattern=useVirtualAgent.test --passWithNoTests</automated>
    <manual>MISSING — 需要集成测试验证 MediaRecorder 和 WebSocket</manual>
  </verify>
  <done>
    - useAgentStore 实现所有状态管理功能
    - useVirtualAgent hook 实现文本和语音消息发送
    - agentApi 实现所有 API 调用
    - WebSocket 连接支持实时异常提示
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 创建虚拟坐席界面组件</name>
  <files>
    src/main/react/components/virtual-agent/VirtualAgentInterface.tsx
    src/main/react/components/virtual-agent/VideoPlayer.tsx
    src/main/react/components/virtual-agent/ChatBubble.tsx
    src/main/react/components/virtual-agent/InputArea.tsx
    src/main/react/components/virtual-agent/ProgressBar.tsx
    src/main/react/components/virtual-agent/ExceptionAlert.tsx
  </files>
  <action>
    创建虚拟坐席界面组件：

    1. **VirtualAgentInterface.tsx** (主容器):
       - 布局：顶部进度条 + 中间视频区 + 对话气泡 + 底部输入区
       - 使用 Flexbox 或 Grid 布局
       - 响应式设计：移动端单列，桌面端三列
       - 注入 useVirtualAgent hook
       - 订阅 useAgentStore 状态

    2. **VideoPlayer.tsx** (虚拟坐席视频):
       - 播放 TTS 动画或虚拟人视频
       - 支持自动播放和循环
       - 显示虚拟坐席头像 (无视频时)
       - 添加 lip-sync 效果 (音频播放时头像微动)

    3. **ChatBubble.tsx** (对话气泡):
       - 区分 agent 和 customer 消息 (不同颜色和对齐)
       - 显示消息时间戳
       - 支持播放音频消息 (点击播放按钮)
       - 添加动画效果 (消息出现时淡入)

    4. **InputArea.tsx** (输入区域):
       - 语音按钮：按住说话 (onMouseDown/onMouseUp 或 onTouchStart/onTouchEnd)
       - 文本输入框：支持回车发送
       - 显示录音状态 (波纹动画)
       - 禁用状态：处理中时禁用输入

    5. **ProgressBar.tsx** (进度条):
       - 显示当前环节名称
       - 显示总体进度百分比
       - 使用阶梯式进度条 (步骤 1→2→3→4)
       - 高亮当前步骤

    6. **ExceptionAlert.tsx** (异常提示):
       - 根据 severity 显示不同颜色 (warning=黄色，error=红色)
       - 显示异常图标和消息
       - 支持自动消失 (5 秒后)
       - 支持手动关闭

    7. **样式**:
       - 使用 Tailwind CSS
       - 配色方案：主色蓝色 (#3B82F6)，成功绿色，警告黄色，错误红色
       - 字体：Inter 或系统字体
       - 圆角：8px (卡片)，9999px (按钮)
       - 阴影：中等阴影 (shadow-md)

    8. **可访问性**:
       - 添加 aria-label 到按钮
       - 支持键盘导航 (Tab 键切换)
       - 颜色对比度符合 WCAG AA 标准
  </action>
  <verify>
    <automated>npm run build -- --dry-run</automated>
    <manual>
      访问 http://localhost:3000/agent/test-session 验证:
      1. 界面布局正确 (视频区 + 对话气泡 + 输入区)
      2. 进度条显示当前环节和总体进度
      3. 语音按钮按住录音，松开停止并发送
      4. 文本输入框可输入并回车发送
      5. 对话气泡显示 agent 和 customer 消息
      6. 异常提示正确显示并自动消失
      7. 移动端响应式布局正确
    </manual>
  </verify>
  <done>
    - VirtualAgentInterface 组件渲染正确
    - VideoPlayer 可播放视频/显示头像
    - ChatBubble 显示对话历史
    - InputArea 支持语音和文本输入
    - ProgressBar 显示流程进度
    - ExceptionAlert 显示异常提示
    - 界面响应式，支持移动端
  </done>
</task>

<task type="auto">
  <name>Task 3: 集成测试和异常处理</name>
  <files>
    src/main/react/components/virtual-agent/VirtualAgentInterface.test.tsx
    src/main/react/hooks/useVirtualAgent.test.ts
    src/main/react/utils/audioUtils.ts
  </files>
  <action>
    完成集成测试和异常处理：

    1. **音频工具函数** (src/main/react/utils/audioUtils.ts):
       - blobToBase64(blob: Blob): Promise<string>
       - base64ToBlob(base64: string, mimeType: string): Blob
       - getAudioDuration(blob: Blob): Promise<number>
       - compressAudio(blob: Blob, quality: number): Promise<Blob>

    2. **组件测试** (VirtualAgentInterface.test.tsx):
       - 渲染测试：组件可正确渲染所有子组件
       - 交互测试：点击语音按钮触发录音
       - 消息测试：发送消息后显示在对话历史中
       - 异常测试：收到异常提示时正确显示
       - Mock API 调用和 WebSocket

    3. **Hook 测试** (useVirtualAgent.test.ts):
       - sendTextMessage 测试：调用 API 并更新 store
       - sendAudioMessage 测试：转换音频并调用 API
       - 状态测试：录音状态、处理状态正确更新
       - 错误测试：API 失败时抛出错误

    4. **异常处理增强**:
       - 网络异常：检测 navigator.onLine，离线时提示
       - 录音权限：catch MediaRecorder 权限错误，提示客户授权
       - 超时处理：API 调用超过 10 秒显示"坐席正在思考..."
       - 重试逻辑：API 失败自动重试 1 次

    5. **性能优化**:
       - 音频压缩：录制后压缩到 64kbps，减少传输大小
       - 虚拟滚动：对话历史超过 50 条时启用虚拟滚动
       - 图片懒加载：虚拟坐席头像懒加载
       - 防抖：文本输入防抖 (500ms)

    6. **日志记录**:
       - 记录用户操作 (开始录音、发送消息)
       - 记录 API 调用 (请求/响应时间)
       - 记录异常事件 (类型、时间、处理结果)
       - 使用 React Query Devtools 调试
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=virtual-agent --passWithNoTests</automated>
  </verify>
  <done>
    - 音频工具函数实现完整
    - 组件测试覆盖主要交互场景
    - Hook 测试覆盖所有公共方法
    - 异常处理完善 (网络、权限、超时)
    - 性能优化生效 (音频压缩、虚拟滚动)
  </done>
</task>

</tasks>

<verification>
**自动化验证**:
1. `npm test -- --testPathPattern=virtual-agent` - 虚拟坐席相关测试通过
2. `npm run build` - 构建成功无错误
3. `npm run lint` - 代码风格检查通过

**手动验证**:
1. 访问 http://localhost:3000/agent/test-session
2. 测试语音输入：按住说话→松开→发送→显示回复
3. 测试文本输入：输入文字→回车→发送→显示回复
4. 测试进度条：显示当前环节和总体进度
5. 测试异常提示：模拟异常，验证提示正确显示
6. 测试响应式：调整浏览器窗口，验证布局正确
</verification>

<success_criteria>
1. ✅ VirtualAgentInterface 组件渲染正确
2. ✅ 支持语音和文本两种输入方式
3. ✅ 对话气泡正确显示 agent 和 customer 消息
4. ✅ 进度条显示当前环节和总体进度
5. ✅ 异常提示实时显示并自动消失
6. ✅ 移动端响应式布局正确
7. ✅ 录音功能正常工作 (MediaRecorder API)
8. ✅ WebSocket 连接支持实时异常推送
9. ✅ 单元测试覆盖率 ≥ 70%
</success_criteria>

<output>
After completion, create `.planning/phases/06-agent-domain/06-03-SUMMARY.md` with:
- Implementation details for each task
- Screenshot of the virtual agent interface
- Test coverage report
- Any deviations from the plan
- Code statistics (lines added/removed)
- Artifacts created (components, hooks, store)
</output>
