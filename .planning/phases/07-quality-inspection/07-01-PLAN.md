---
phase: 07-quality-inspection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: 
  - aidrclaw-core/src/main/java/com/aidrclaw/quality/model/QualityReport.java
  - aidrclaw-core/src/main/java/com/aidrclaw/quality/model/ScriptCompletionResult.java
  - aidrclaw-core/src/main/java/com/aidrclaw/quality/model/ViolationRecord.java
  - aidrclaw-core/src/test/java/com/aidrclaw/quality/model/QualityReportTest.java
autonomous: true
requirements: [AGENT-03, AI-05]

must_haves:
  truths:
    - "质检报告可被序列化为 JSON"
    - "话术完成结果包含完成率和通过状态"
    - "违规记录包含词汇和严重程度"
  artifacts:
    - path: "aidrclaw-core/src/main/java/com/aidrclaw/quality/model/QualityReport.java"
      provides: "质检报告数据模型"
      contains: "score, passed, reason, violations"
    - path: "aidrclaw-core/src/main/java/com/aidrclaw/quality/model/ScriptCompletionResult.java"
      provides: "话术完成结果"
      contains: "completionRate, passed"
    - path: "aidrclaw-core/src/main/java/com/aidrclaw/quality/model/ViolationRecord.java"
      provides: "违规记录"
      contains: "word, severity, timestamp"
  key_links:
    - from: "QualityReport"
      to: "ViolationRecord"
      via: "List<ViolationRecord> violations"
      pattern: "List<ViolationRecord>"
---

<objective>
创建质检数据模型，包括质检报告、话术完成结果、违规记录
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-quality-inspection/07-CONTEXT.md
@.planning/phases/07-quality-inspection/07-RESEARCH.md
@.planning/ROADMAP.md

@aidrclaw-core/src/main/java/com/aidrclaw/agent/state/DialogState.java
@aidrclaw-core/src/main/java/com/aidrclaw/agent/rule/BusinessRule.java
</context>

<interfaces>
<!-- 现有模型参考 - 质检模型应保持一致的 Lombok 风格 -->

From DialogState.java:
```java
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class DialogState {
    private String sessionId;
    private List<String> history;
    private Map<String, Object> variables;
    // ...
}
```

From BusinessRule.java:
```java
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class BusinessRule {
    private String id;
    private String name;
    private RuleCategory category;
    // ...
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 QualityReport 质检报告模型</name>
  <files>
    aidrclaw-core/src/main/java/com/aidrclaw/quality/model/QualityReport.java
    aidrclaw-core/src/test/java/com/aidrclaw/quality/model/QualityReportTest.java
  </files>
  <action>
创建 QualityReport 类，包含以下字段：
- score: Integer (0-100 分)
- passed: Boolean (是否通过)
- reason: String (不通过原因)
- violations: List&lt;ViolationRecord&gt; (违规记录列表)
- inspectionTime: LocalDateTime (质检时间)
- durationMs: Long (质检耗时毫秒)

添加静态工厂方法：
- timeout(String message): 创建超时报告

遵循现有模型风格：@Data, @Builder, @NoArgsConstructor, @AllArgsConstructor

TDD 要求：
1. 先创建测试类 QualityReportTest
2. 测试 Builder 模式构建报告
3. 测试 timeout 静态方法
4. 测试 JSON 序列化（使用 Jackson）
  </action>
  <verify>
    <automated>mvn test -Dtest=QualityReportTest -pl aidrclaw-core</automated>
  </verify>
  <done>QualityReport 类创建完成，所有测试通过，可正确序列化为 JSON</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建 ScriptCompletionResult 话术完成结果模型</name>
  <files>
    aidrclaw-core/src/main/java/com/aidrclaw/quality/model/ScriptCompletionResult.java
    aidrclaw-core/src/test/java/com/aidrclaw/quality/model/ScriptCompletionResultTest.java
  </files>
  <action>
创建 ScriptCompletionResult 类，包含以下字段：
- completionRate: double (完成率 0.0-1.0)
- completedCount: int (已完成节点数)
- totalCount: int (总节点数)
- passed: Boolean (是否通过，完成率≥80%)
- missingNodes: List&lt;String&gt; (缺失的节点 ID 列表)

遵循现有模型风格：@Data, @Builder, @NoArgsConstructor, @AllArgsConstructor

TDD 要求：
1. 先创建测试类 ScriptCompletionResultTest
2. 测试完成率计算逻辑
3. 测试 passed 字段自动计算（阈值 80%）
4. 测试 missingNodes 列表生成
  </action>
  <verify>
    <automated>mvn test -Dtest=ScriptCompletionResultTest -pl aidrclaw-core</automated>
  </verify>
  <done>ScriptCompletionResult 类创建完成，所有测试通过，完成率计算正确</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 创建 ViolationRecord 违规记录模型</name>
  <files>
    aidrclaw-core/src/main/java/com/aidrclaw/quality/model/ViolationRecord.java
    aidrclaw-core/src/test/java/com/aidrclaw/quality/model/ViolationRecordTest.java
  </files>
  <action>
创建 ViolationRecord 类，包含以下字段：
- word: String (违规词)
- severity: ViolationSeverity (枚举：GENERAL, SEVERE)
- timestamp: LocalDateTime (发现时间)
- context: String (上下文，违规词出现的前后文)

创建 ViolationSeverity 枚举：
- GENERAL (一般违规，扣 5 分)
- SEVERE (严重违规，扣 20 分)

TDD 要求：
1. 先创建测试类 ViolationRecordTest
2. 测试枚举值
3. 测试 Builder 构建
4. 测试 JSON 序列化
  </action>
  <verify>
    <automated>mvn test -Dtest=ViolationRecordTest -pl aidrclaw-core</automated>
  </verify>
  <done>ViolationRecord 类和 ViolationSeverity 枚举创建完成，所有测试通过</done>
</task>

</tasks>

<verification>
- [ ] 所有模型类遵循 Lombok 模式（与 DialogState、BusinessRule 一致）
- [ ] 所有模型类可正确序列化/反序列化为 JSON
- [ ] 单元测试覆盖率 ≥ 80%
- [ ] 代码通过 mvn checkstyle:check 检查
</verification>

<success_criteria>
1. ✅ QualityReport、ScriptCompletionResult、ViolationRecord 三个模型类创建完成
2. ✅ 所有测试通过（mvn test -pl aidrclaw-core）
3. ✅ 模型可被 Jackson 正确序列化
4. ✅ 代码风格与现有代码库一致
</success_criteria>

<output>
After completion, create `.planning/phases/07-quality-inspection/07-01-SUMMARY.md`
</output>
