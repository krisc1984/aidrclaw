---
phase: 02-capture-plugins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [aidrclaw-plugins-capture-web/src/main/java/com/aidrclaw/plugin/capture/web/WebCapturePlugin.java, aidrclaw-plugins-capture-web/pom.xml, aidrclaw-web-capture-frontend/src/components/WebRTCPreview.tsx, aidrclaw-plugins-capture-web/src/main/resources/META-INF/services/com.aidrclaw.plugin.Plugin]
autonomous: true
requirements: [CAPTURE-02]

must_haves:
  truths:
    - "用户打开网页可看到摄像头预览画面"
    - "用户点击开始录制可采集音视频流"
    - "录制中的音视频数据可传递给后端插件"
  artifacts:
    - path: "aidrclaw-plugins-capture-web/src/main/java/com/aidrclaw/plugin/capture/web/WebCapturePlugin.java"
      provides: "Web 采集插件后端实现"
      exports: ["WebCapturePlugin (implements Plugin)"]
    - path: "aidrclaw-web-capture-frontend/src/components/WebRTCPreview.tsx"
      provides: "WebRTC 预览组件"
      exports: ["WebRTCPreview (React Component)"]
    - path: "aidrclaw-web-capture-frontend/src/hooks/useWebRTC.ts"
      provides: "WebRTC 采集逻辑封装"
      exports: ["useWebRTC (custom hook)"]
  key_links:
    - from: "aidrclaw-web-capture-frontend/src/hooks/useWebRTC.ts"
      to: "navigator.mediaDevices.getUserMedia()"
      via: "浏览器原生 API 调用"
      pattern: "navigator\\.mediaDevices\\.getUserMedia"
    - from: "aidrclaw-plugins-capture-web/WebCapturePlugin.java"
      to: "com.aidrclaw.plugin.Plugin"
      via: "插件接口实现"
      pattern: "implements Plugin"
---

<objective>
实现 Web 端 WebRTC 音视频采集插件，支持浏览器端无需安装插件即可采集摄像头和麦克风
Purpose: 满足 CAPTURE-02 需求，提供零安装的 Web 录制能力
Output: Web 采集插件（后端）+ WebRTC 前端组件
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-basic-architecture/01-SUMMARY.md
@.planning/phases/02-capture-plugins/02-CONTEXT.md

# Phase 1 交付的插件 API
<interfaces>
From aidrclaw-plugin-api/Plugin.java:
```java
public interface Plugin {
    void init(PluginContext context);
    PluginResult execute(PluginContext context);
    void destroy();
    PluginMetadata getMetadata();
}
```

From aidrclaw-plugin-api/PluginContext.java:
```java
public class PluginContext {
    private Map<String, String> config;
    private Map<String, Object> parameters;
    // getters/setters
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 Web 采集插件模块</name>
  <files>aidrclaw-plugins-capture-web/pom.xml, aidrclaw-plugins-capture-web/src/main/java/com/aidrclaw/plugin/capture/web/WebCapturePlugin.java</files>
  <behavior>
    - Test: WebCapturePlugin 可实现 Plugin 接口
    - Test: getMetadata() 返回正确的插件信息 (id: "web-capture", version: "1.0.0")
    - Test: execute() 方法可接收前端上传的音视频流
    - Test: 插件可通过 SPI 机制被发现 (META-INF/services 注册)
  </behavior>
  <action>
    1. 创建 Maven 子模块 `aidrclaw-plugins-capture-web`，继承父 POM
    2. 添加依赖：aidrclaw-plugin-api, Spring Web, Commons IO
    3. 实现 WebCapturePlugin 类：
       - 实现 Plugin 接口
       - getMetadata() 返回插件元数据
       - execute() 方法处理前端上传的音视频流（保存到临时目录）
    4. 创建 META-INF/services/com.aidrclaw.plugin.Plugin 文件，注册 WebCapturePlugin
    5. 编写单元测试验证插件可被 ServiceLoader 发现
  </action>
  <verify>
    <automated>mvn test -pl aidrclaw-plugins-capture-web -Dtest=WebCapturePluginTest</automated>
  </verify>
  <done>Web 采集插件模块编译通过，单元测试通过，SPI 注册完成</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 WebRTC 前端采集组件</name>
  <files>aidrclaw-web-capture-frontend/src/hooks/useWebRTC.ts, aidrclaw-web-capture-frontend/src/components/WebRTCPreview.tsx</files>
  <behavior>
    - Test: useWebRTC hook 可请求摄像头权限
    - Test: 获取权限后可显示视频预览
    - Test: 开始录制时可调用 getUserMedia 获取媒体流
    - Test: 错误场景（拒绝权限）时返回友好错误信息
  </behavior>
  <action>
    1. 创建 React 前端模块 `aidrclaw-web-capture-frontend`（Vite + React + TypeScript）
    2. 实现 useWebRTC hook：
       - 请求摄像头/麦克风权限（使用 navigator.mediaDevices.getUserMedia）
       - 返回 MediaStream 对象
       - 处理权限拒绝、设备不可用等错误
    3. 实现 WebRTCPreview 组件：
       - 使用 video 标签显示摄像头预览
       - 绑定 MediaStream 到 video.srcObject
       - 显示设备状态（权限请求中、已授权、已拒绝）
    4. 编写测试验证 hook 可获取 MediaStream
  </action>
  <verify>
    <automated>npm test -- --filter=useWebRTC</automated>
  </verify>
  <done>WebRTC hook 和预览组件完成，可在浏览器显示摄像头画面</done>
</task>

<task type="auto">
  <name>Task 3: 实现音视频流上传逻辑</name>
  <files>aidrclaw-web-capture-frontend/src/components/RecordingControls.tsx, aidrclaw-plugins-capture-web/src/main/java/com/aidrclaw/plugin/capture/web/api/UploadController.java</files>
  <action>
    1. 实现 RecordingControls 组件：
       - "开始录制"按钮：调用 MediaRecorder API 开始录制
       - "停止录制"按钮：停止录制并触发上传
       - 使用 MediaRecorder 录制 MediaStream 为 WebM 格式
    2. 实现 UploadController：
       - POST /api/capture/web/upload 接收文件上传
       - 使用 MultipartFile 接收 WebM 文件
       - 调用 WebCapturePlugin.execute() 处理文件
       - 返回上传结果 {success: boolean, fileId: string}
    3. 前端上传逻辑：
       - 录制停止后自动上传 Blob 到后端
       - 显示上传进度
       - 上传成功后显示确认信息
  </action>
  <verify>
    <automated>curl -X POST http://localhost:8080/api/capture/web/upload -F "file=@test.webm" | jq</automated>
  </verify>
  <done>可录制音视频并上传到后端，后端可接收并保存文件</done>
</task>

</tasks>

<verification>
- Web 前端可在浏览器显示摄像头预览
- 点击开始录制可采集音视频
- 停止录制后文件可上传到后端
- 后端插件可接收并保存文件
- 执行 `mvn test` 所有测试通过
</verification>

<success_criteria>
1. Web 端无需安装插件即可采集摄像头
2. 预览画面清晰（720p）
3. 录制文件可成功上传并保存
4. 错误提示友好（权限拒绝、设备不可用）
</success_criteria>

<output>
After completion, create `.planning/phases/02-capture-plugins/02-01-SUMMARY.md`
</output>
