---
phase: 04-ai-plugins
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified: [src/test/java/com/aidrclaw/ai/, ai-services/docker-compose.yml, .planning/phases/04-ai-plugins/]
autonomous: false  # Has checkpoint for user verification
requirements: [AI-01, AI-02, AI-03, AI-04]

must_haves:
  truths:
    - "All 4 AI plugins can be loaded and initialized via PluginManager"
    - "End-to-end flow: capture → AI processing → storage works"
    - "Performance meets targets (ASR <1s, TTS <500ms, Face <200ms, Liveness <500ms)"
    - "All AI services start together via Docker Compose"
  artifacts:
    - path: "src/test/java/com/aidrclaw/ai/AiIntegrationTest.java"
      provides: "End-to-end integration tests"
      min_lines: 150
    - path: "ai-services/docker-compose.yml"
      provides: "All AI services orchestration"
      contains: "ai-asr, ai-tts, ai-face"
    - path: ".planning/phases/04-ai-plugins/04-VALIDATION.md"
      provides: "Validation report with accuracy metrics"
      contains: "accuracy results"
  key_links:
    - from: "AiIntegrationTest.testEndToEnd()"
      to: "AsrPlugin, TtsPlugin, FacePlugin"
      via: "PluginManager loading"
      pattern: "pluginManager\\.getPlugin\\("
    - from: "ai-services/docker-compose.yml"
      to: "All AI service definitions"
      via: "service dependencies"
      pattern: "depends_on:.*ai-"
---

<objective>
Integrate all AI plugins, run end-to-end tests, and validate performance/accuracy targets

Purpose: Ensure all 4 AI capabilities work together and meet compliance requirements
Output: Validated AI plugin suite ready for Phase 5 integration
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ai-plugins/04-CONTEXT.md
@.planning/phases/04-ai-plugins/04-RESEARCH.md
@.planning/phases/04-ai-plugins/04-01-PLAN.md
@.planning/phases/04-ai-plugins/04-02-PLAN.md
@.planning/phases/04-ai-plugins/04-03-PLAN.md
@.planning/REQUIREMENTS.md

# Integration points
@src/main/java/com/aidrclaw/plugin/PluginManager.java
@src/main/java/com/aidrclaw/capture/WebCapturePlugin.java
@src/main/java/com/aidrclaw/storage/StorageFile.java
</context>

<interfaces>
<!-- PluginManager interface for loading plugins -->

From Phase 1:
```java
public class PluginManager {
    // Load all plugins from classpath
    public void loadPlugins();
    
    // Get plugin by name
    public Plugin getPlugin(String name);
    
    // Execute plugin
    public PluginResult executePlugin(String name, PluginContext context);
}
```

Test data requirements:
```java
// Test audio file (10s, financial script)
byte[] testAudio = loadResource("test-audio-financial.wav");

// Test images
byte[] customerFace = loadResource("test-face-customer.jpg");
byte[] idCardFace = loadResource("test-face-idcard.jpg");
byte[] photoAttack = loadResource("test-attack-photo.jpg");
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create AI services Docker Compose orchestration</name>
  <files>ai-services/docker-compose.yml, ai-services/.env</files>
  <read_first>
    - docker-compose.yml (root level, for integration pattern)
    - ai-services/asr/Dockerfile
    - ai-services/tts/Dockerfile
    - ai-services/face/Dockerfile
  </read_first>
  <action>
    Create unified Docker Compose for all AI services:
    
    1. Create ai-services/docker-compose.yml with:
       - Version: '3.8'
       - Services:
         * ai-asr (from Plan 04-01)
         * ai-tts (from Plan 04-02)
         * ai-face (from Plan 04-02)
       - Shared network: ai-network
       - Shared volume: ai-models (for model caching)
       - Health checks for each service
       - GPU resource reservations (if available)
    
    2. Create ai-services/.env with:
       - ASR_PORT=8000
       - TTS_PORT=8001
       - FACE_PORT=8002
       - GPU_ENABLED=true
    
    3. Add convenience scripts:
         * ai-services/start-all.sh (docker-compose up -d)
         * ai-services/stop-all.sh (docker-compose down)
         * ai-services/logs.sh (docker-compose logs -f)
    
    4. Test: docker-compose up -d should start all 3 services
  </action>
  <verify>
    <automated>cd ai-services && docker-compose config --quiet && echo "COMPOSE_VALID"</automated>
    <manual>docker-compose ps shows all 3 services running</manual>
  </verify>
  <done>All AI services start together via docker-compose, health checks pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement end-to-end integration tests</name>
  <files>src/test/java/com/aidrclaw/ai/AiIntegrationTest.java, src/test/resources/test-audio-financial.wav, src/test/resources/test-*.jpg</files>
  <read_first>
    - src/main/java/com/aidrclaw/plugin/PluginManager.java (how to load plugins)
    - src/test/java/com/aidrclaw/plugin/PluginManagerTest.java (existing test pattern)
    - .planning/phases/04-ai-plugins/04-RESEARCH.md (performance targets)
  </read_first>
  <action>
    Create comprehensive integration tests:
    
    1. Create test resources:
       - test-audio-financial.wav (10s, someone reading financial script in Chinese)
       - test-face-customer.jpg (clear face photo)
       - test-face-idcard.jpg (same person, ID card style)
       - test-face-different.jpg (different person)
       - test-attack-photo.jpg (printed photo for liveness test)
    
    2. Create AiIntegrationTest.java with tests:
       - testPluginLoading(): All 4 plugins load via PluginManager
       - testAsrEndToEnd(): Audio → ASR → text with ≥95% accuracy
       - testFaceComparison(): Same person → match=true, Different → match=false
       - testLivenessDetection(): Real face → pass, Photo → fail
       - testTtsSynthesis(): Text → audio → can play (duration >0)
       - testPerformanceASR(): 10s audio → latency <1s
       - testPerformanceFace(): Face comparison → latency <200ms
       - testPerformanceTTS(): 100 chars → latency <500ms
       - testPerformanceLiveness(): Liveness → latency <500ms
    
    3. Use @SpringBootTest for Spring context
    4. Use @Testcontainers for Docker service management
    5. Add test report generation (HTML or JSON)
  </action>
  <behavior>
    - Test: All plugins load without errors
    - Test: ASR transcribes financial script with ≥95% word accuracy
    - Test: Face comparison correctly identifies same/different people
    - Test: Liveness detects photo attack
    - Test: TTS generates playable audio
    - Test: All performance targets met
  </behavior>
  <verify>
    <automated>mvn test -Dtest=AiIntegrationTest -q</automated>
    <manual>Test report shows all 9 tests passed, performance within targets</manual>
  </verify>
  <done>Integration tests pass, all performance targets met</done>
</task>

<task type="auto">
  <name>Task 3: Create validation report and documentation</name>
  <files>.planning/phases/04-ai-plugins/04-VALIDATION.md, docs/ai-plugins.md</files>
  <read_first>
    - .planning/phases/04-ai-plugins/04-RESEARCH.md (validation criteria)
    - .planning/REQUIREMENTS.md (AI-01 through AI-04 requirements)
  </read_first>
  <action>
    Create validation report:
    
    1. Create .planning/phases/04-ai-plugins/04-VALIDATION.md with:
       - Test date and environment (CPU/GPU, memory)
       - Accuracy results table:
         * ASR: X% (target: 95%)
         * Face: X% (target: 99%)
         * Liveness: X% (target: 98%)
       - Performance results table:
         * ASR latency: X ms (target: <1000ms)
         * Face latency: X ms (target: <200ms)
         * TTS latency: X ms (target: <500ms)
         * Liveness latency: X ms (target: <500ms)
       - MOS evaluation for TTS (5 users, scale 1-5)
       - Pass/fail for each requirement (AI-01 through AI-04)
    
    2. Create docs/ai-plugins.md with:
       - Architecture overview diagram
       - Plugin usage examples
       - Configuration reference
       - Troubleshooting guide
       - Model download instructions
    
    3. Run validation suite and populate results:
       ```bash
       mvn test -Dtest=AiIntegrationTest
       # Parse results into VALIDATION.md
       ```
  </action>
  <verify>
    <automated>grep -q "ASR.*accuracy" .planning/phases/04-ai-plugins/04-VALIDATION.md && echo "VALIDATION_EXISTS"</automated>
    <manual>docs/ai-plugins.md contains architecture diagram and usage examples</manual>
  </verify>
  <done>Validation report shows all requirements met, documentation complete</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete AI plugin suite with 4 plugins:
    - ASR Plugin (speech-to-text)
    - TTS Plugin (text-to-speech)
    - Face Recognition Plugin (identity verification)
    - Liveness Detection Plugin (anti-spoofing)
    
    All services running via Docker Compose, integration tests passing.
  </what-built>
  <how-to-verify>
    1. Start all AI services:
       ```bash
       cd ai-services && docker-compose up -d
       ```
    
    2. Run integration tests:
       ```bash
       mvn test -Dtest=AiIntegrationTest
       ```
    
    3. Verify accuracy targets:
       - Check test output for ASR accuracy ≥95%
       - Check face comparison accuracy ≥99%
       - Check liveness detection ≥98%
    
    4. Verify performance:
       - ASR latency <1s for 10s audio
       - Face comparison <200ms
       - TTS <500ms for 100 chars
       - Liveness <500ms
    
    5. Review validation report:
       - cat .planning/phases/04-ai-plugins/04-VALIDATION.md
    
    6. Test manually (optional):
       - Call ASR API with audio file → verify text output
       - Call Face API with photos → verify similarity score
       - Call TTS API with text → listen to audio quality
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass and accuracy/performance targets are met, or describe any failures</resume-signal>
</task>

</tasks>

<verification>
- [ ] All 3 AI services (ASR, TTS, Face) start via docker-compose
- [ ] All 4 plugins load via PluginManager
- [ ] Integration tests pass (9/9 tests)
- [ ] ASR accuracy ≥95% on financial speech
- [ ] Face comparison accuracy ≥99%
- [ ] Liveness detection ≥98% (detects photo attacks)
- [ ] All performance targets met (latency)
- [ ] TTS MOS≥4.0 (subjective evaluation)
- [ ] Validation report created with results
- [ ] Documentation complete
</verification>

<success_criteria>
1. All 4 AI plugins functional and tested
2. Integration tests verify end-to-end flows
3. Performance targets met for all plugins
4. Accuracy targets met for compliance
5. Docker Compose orchestrates all services
6. Documentation enables future maintenance
7. User validates results via checkpoint
</success_criteria>

<output>
After completion, create .planning/phases/04-ai-plugins/04-04-SUMMARY.md with:
- Integration test results (all 9 tests)
- Accuracy metrics (ASR, Face, Liveness)
- Performance metrics (latency for all)
- Validation status (PASS/FAIL per requirement)
- Links to documentation
- User approval from checkpoint
</output>
