---
phase: 08-admin-client
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: 
  - admin-react/src/pages/admin/RecordList.tsx
  - admin-react/src/pages/admin/QualityReview.tsx
  - admin-react/src/components/admin/VideoPlayer.tsx
autonomous: true
requirements: [ADMIN-03, ADMIN-04]

must_haves:
  truths:
    - "录制记录列表可展示"
    - "支持按维度筛选"
    - "音视频可在线回放"
    - "质检结果可复核"
  artifacts:
    - path: "admin-react/src/pages/admin/RecordList.tsx"
      provides: "录制记录列表页面"
      exports: ["RecordList"]
    - path: "admin-react/src/components/admin/VideoPlayer.tsx"
      provides: "音视频播放器组件"
      exports: ["VideoPlayer"]
    - path: "admin-react/src/pages/admin/QualityReview.tsx"
      provides: "质检复核页面"
      exports: ["QualityReview"]
  key_links:
    - from: "RecordList"
      to: "/api/admin/records"
      via: "axios GET"
      pattern: "axios.get.*records"
---

<objective>
实现录制记录查询和质检复核页面
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/references/ui-brand.md
</execution_context>

<context>
@.planning/phases/08-admin-client/08-CONTEXT.md
@.planning/phases/08-admin-client/08-RESEARCH.md

@aidrclaw-core/src/main/java/com/aidrclaw/quality/agent/QualityInspectionAgent.java
@aidrclaw-core/src/main/java/com/aidrclaw/quality/model/QualityReport.java
</context>

<interfaces>
<!-- 录制记录 API -->

```typescript
interface RecordingRecord {
  id: string;
  sessionId: string;
  customerName: string;
  product: string;
  startTime: string;
  duration: number;
  videoUrl: string;
  qualityScore?: number;
  qualityPassed?: boolean;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 实现录制记录列表页面</name>
  <files>
    admin-react/src/pages/admin/RecordList.tsx
    admin-react/src/types/recording.ts
  </files>
  <action>
创建录制记录列表页面（支持搜索和筛选）：

```typescript
// RecordList.tsx
import { useEffect, useState } from 'react';
import axios from 'axios';
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table';
import { Input } from '@/components/ui/input';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import { Button } from '@/components/ui/button';
import { VideoPlayer } from '@/components/admin/VideoPlayer';

interface Recording {
  id: string;
  sessionId: string;
  customerName: string;
  product: string;
  startTime: string;
  duration: number;
  videoUrl: string;
  qualityScore?: number;
  qualityPassed?: boolean;
}

export const RecordList = () => {
  const [records, setRecords] = useState<Recording[]>([]);
  const [searchTerm, setSearchTerm] = useState('');
  const [productFilter, setProductFilter] = useState('all');
  const [statusFilter, setStatusFilter] = useState('all');
  const [playingUrl, setPlayingUrl] = useState<string | null>(null);
  
  useEffect(() => {
    axios.get('/api/admin/records').then(res => setRecords(res.data));
  }, []);
  
  const filteredRecords = records.filter(record => {
    const matchesSearch = record.customerName.includes(searchTerm) || 
                         record.sessionId.includes(searchTerm);
    const matchesProduct = productFilter === 'all' || record.product === productFilter;
    const matchesStatus = statusFilter === 'all' || 
                         (statusFilter === 'passed' && record.qualityPassed) ||
                         (statusFilter === 'failed' && !record.qualityPassed);
    return matchesSearch && matchesProduct && matchesStatus;
  });
  
  return (
    <div>
      <h1 className="text-2xl font-bold mb-4">录制记录</h1>
      
      {/* 筛选栏 */}
      <div className="flex gap-4 mb-4">
        <Input 
          placeholder="搜索客户名称或会话 ID" 
          value={searchTerm}
          onChange={e => setSearchTerm(e.target.value)}
          className="max-w-sm"
        />
        <Select value={productFilter} onValueChange={setProductFilter}>
          <SelectTrigger className="w-[180px]">
            <SelectValue placeholder="产品类型" />
          </SelectTrigger>
          <SelectContent>
            <SelectItem value="all">全部产品</SelectItem>
            <SelectItem value="fund">基金</SelectItem>
            <SelectItem value="insurance">保险</SelectItem>
            <SelectItem value="wealth">理财</SelectItem>
          </SelectContent>
        </Select>
        <Select value={statusFilter} onValueChange={setStatusFilter}>
          <SelectTrigger className="w-[180px]">
            <SelectValue placeholder="质检状态" />
          </SelectTrigger>
          <SelectContent>
            <SelectItem value="all">全部状态</SelectItem>
            <SelectItem value="passed">通过</SelectItem>
            <SelectItem value="failed">不通过</SelectItem>
          </SelectContent>
        </Select>
      </div>
      
      {/* 记录列表 */}
      <Table>
        <TableHeader>
          <TableRow>
            <TableHead>会话 ID</TableHead>
            <TableHead>客户名称</TableHead>
            <TableHead>产品</TableHead>
            <TableHead>开始时间</TableHead>
            <TableHead>时长</TableHead>
            <TableHead>质检分数</TableHead>
            <TableHead>操作</TableHead>
          </TableRow>
        </TableHeader>
        <TableBody>
          {filteredRecords.map(record => (
            <TableRow key={record.id}>
              <TableCell className="font-mono text-sm">{record.sessionId}</TableCell>
              <TableCell>{record.customerName}</TableCell>
              <TableCell>{record.product}</TableCell>
              <TableCell>{new Date(record.startTime).toLocaleString()}</TableCell>
              <TableCell>{Math.floor(record.duration / 60)}:{String(record.duration % 60).padStart(2, '0')}</TableCell>
              <TableCell>
                {record.qualityScore !== undefined && (
                  <span className={record.qualityPassed ? 'text-green-600' : 'text-red-600'}>
                    {record.qualityScore}分
                  </span>
                )}
              </TableCell>
              <TableCell>
                <Button 
                  variant="outline" 
                  size="sm"
                  onClick={() => setPlayingUrl(playingUrl === record.videoUrl ? null : record.videoUrl)}
                >
                  {playingUrl === record.videoUrl ? '关闭' : '回放'}
                </Button>
              </TableCell>
            </TableRow>
          ))}
        </TableBody>
      </Table>
      
      {/* 视频回放 */}
      {playingUrl && (
        <div className="fixed inset-0 bg-black/80 flex items-center justify-center z-50">
          <div className="bg-white p-4 rounded-lg max-w-4xl w-full">
            <VideoPlayer src={playingUrl} />
            <Button onClick={() => setPlayingUrl(null)} className="mt-4">关闭</Button>
          </div>
        </div>
      )}
    </div>
  );
};
```
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>录制记录列表页面创建完成，支持搜索和筛选</done>
</task>

<task type="auto">
  <name>Task 2: 实现音视频播放器组件</name>
  <files>admin-react/src/components/admin/VideoPlayer.tsx</files>
  <action>
创建音视频播放器组件：

```typescript
// VideoPlayer.tsx
export const VideoPlayer = ({ src }: { src: string }) => {
  return (
    <video controls className="w-full h-auto max-h-[70vh]" autoPlay>
      <source src={src} type="video/mp4" />
      您的浏览器不支持视频播放
    </video>
  );
};
```
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>音视频播放器创建完成</done>
</task>

<task type="auto">
  <name>Task 3: 实现质检复核页面</name>
  <files>admin-react/src/pages/admin/QualityReview.tsx</files>
  <action>
创建质检复核页面（展示质检结果，支持修改结论）：

```typescript
// QualityReview.tsx
import { useEffect, useState } from 'react';
import axios from 'axios';
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table';
import { Button } from '@/components/ui/button';
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from '@/components/ui/dialog';
import { Textarea } from '@/components/ui/textarea';
import { Badge } from '@/components/ui/badge';

interface QualityResult {
  id: string;
  sessionId: string;
  customerName: string;
  score: number;
  passed: boolean;
  reason?: string;
  violations: string[];
  reviewStatus: 'pending' | 'approved' | 'rejected';
  reviewComment?: string;
}

export const QualityReview = () => {
  const [results, setResults] = useState<QualityResult[]>([]);
  const [selectedResult, setSelectedResult] = useState<QualityResult | null>(null);
  const [reviewComment, setReviewComment] = useState('');
  
  useEffect(() => {
    axios.get('/api/admin/quality/results').then(res => setResults(res.data));
  }, []);
  
  const submitReview = async (resultId: string, approved: boolean) => {
    await axios.post(`/api/admin/quality/${resultId}/review`, {
      approved,
      comment: reviewComment,
    });
    setResults(results.map(r => r.id === resultId ? { 
      ...r, 
      reviewStatus: approved ? 'approved' : 'rejected',
      reviewComment 
    } : r));
    setSelectedResult(null);
  };
  
  return (
    <div>
      <h1 className="text-2xl font-bold mb-4">质检复核</h1>
      
      <Table>
        <TableHeader>
          <TableRow>
            <TableHead>会话 ID</TableHead>
            <TableHead>客户名称</TableHead>
            <TableHead>质检分数</TableHead>
            <TableHead>结果</TableHead>
            <TableHead>违规词</TableHead>
            <TableHead>复核状态</TableHead>
            <TableHead>操作</TableHead>
          </TableRow>
        </TableHeader>
        <TableBody>
          {results.map(result => (
            <TableRow key={result.id}>
              <TableCell className="font-mono text-sm">{result.sessionId}</TableCell>
              <TableCell>{result.customerName}</TableCell>
              <TableCell>{result.score}</TableCell>
              <TableCell>
                <Badge variant={result.passed ? 'default' : 'destructive'}>
                  {result.passed ? '通过' : '不通过'}
                </Badge>
              </TableCell>
              <TableCell>{result.violations.join(', ')}</TableCell>
              <TableCell>
                <Badge variant={
                  result.reviewStatus === 'approved' ? 'default' :
                  result.reviewStatus === 'rejected' ? 'destructive' : 'secondary'
                }>
                  {result.reviewStatus === 'pending' ? '待复核' : 
                   result.reviewStatus === 'approved' ? '已通过' : '已拒绝'}
                </Badge>
              </TableCell>
              <TableCell>
                <Dialog>
                  <DialogTrigger asChild>
                    <Button 
                      variant="outline" 
                      size="sm"
                      disabled={result.reviewStatus !== 'pending'}
                      onClick={() => setSelectedResult(result)}
                    >
                      复核
                    </Button>
                  </DialogTrigger>
                  <DialogContent>
                    <DialogHeader>
                      <DialogTitle>质检复核 - {result.sessionId}</DialogTitle>
                    </DialogHeader>
                    <div className="space-y-4">
                      <div>
                        <div className="font-bold">质检分数：{result.score}</div>
                        <div className="font-bold">结果：{result.passed ? '通过' : '不通过'}</div>
                        <div className="font-bold">原因：{result.reason}</div>
                      </div>
                      <div>
                        <div className="font-bold">违规词：</div>
                        <div>{result.violations.join(', ')}</div>
                      </div>
                      <Textarea 
                        placeholder="复核意见" 
                        value={reviewComment}
                        onChange={e => setReviewComment(e.target.value)}
                      />
                      <div className="flex gap-2">
                        <Button onClick={() => submitReview(result.id, true)} variant="default">
                          通过
                        </Button>
                        <Button onClick={() => submitReview(result.id, false)} variant="destructive">
                          拒绝
                        </Button>
                      </div>
                    </div>
                  </DialogContent>
                </Dialog>
              </TableCell>
            </TableRow>
          ))}
        </TableBody>
      </Table>
    </div>
  );
};
```
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>质检复核页面创建完成，支持查看和修改质检结论</done>
</task>

</tasks>

<verification>
- [ ] 录制记录列表可展示
- [ ] 支持按客户名称、产品、状态筛选
- [ ] 音视频可在线回放
- [ ] 质检复核页面可展示结果
- [ ] 支持修改质检结论
- [ ] 编译通过
</verification>

<success_criteria>
1. ✅ 录制记录列表完成
2. ✅ 音视频播放器完成
3. ✅ 质检复核页面完成
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-client/08-03-SUMMARY.md`
</output>
