---
phase: 08-admin-client
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: 
  - admin-react/src/pages/admin/PluginMarket.tsx
  - admin-react/src/components/admin/AdminDashboard.tsx
  - admin-react/src/routes.tsx
autonomous: true
requirements: [ADMIN-01]

must_haves:
  truths:
    - "管理后台路由配置正确"
    - "插件市场可展示已安装插件"
    - "支持启用/停用插件"
  artifacts:
    - path: "admin-react/src/components/admin/AdminDashboard.tsx"
      provides: "管理后台主布局"
      contains: "Sidebar, Header, Outlet"
    - path: "admin-react/src/pages/admin/PluginMarket.tsx"
      provides: "插件市场页面"
      exports: ["PluginMarket"]
  key_links:
    - from: "PluginMarket"
      to: "/api/admin/plugins"
      via: "axios GET"
      pattern: "axios.get.*plugins"
---

<objective>
实现管理后台基础架构和插件市场页面
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/references/ui-brand.md
</execution_context>

<context>
@.planning/phases/08-admin-client/08-CONTEXT.md
@.planning/phases/08-admin-client/08-RESEARCH.md

@aidrclaw-core/src/main/java/com/aidrclaw/plugin/PluginManager.java
@aidrclaw-core/src/main/resources/application.yml
</context>

<interfaces>
<!-- 后端插件管理 API -->

From PluginManager.java:
```java
public List<PluginInfo> getAllPlugins();
public void togglePlugin(String id, boolean enabled);
public PluginInfo getPluginConfig(String id);
public void updatePluginConfig(String id, Map<String, Object> config);
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建管理后台主布局和路由</name>
  <files>
    admin-react/src/components/admin/AdminDashboard.tsx
    admin-react/src/routes.tsx
  </files>
  <action>
创建管理后台主布局（侧边栏 + 顶栏 + 内容区）：

```typescript
// AdminDashboard.tsx
import { Outlet } from 'react-router-dom';

export const AdminDashboard = () => {
  return (
    <div className="flex h-screen bg-gray-100">
      {/* 侧边栏 */}
      <aside className="w-64 bg-white border-r">
        <nav className="p-4">
          <Link to="/admin/plugins" className="block p-2 hover:bg-gray-100">插件市场</Link>
          <Link to="/admin/flows" className="block p-2 hover:bg-gray-100">流程设计器</Link>
          <Link to="/admin/records" className="block p-2 hover:bg-gray-100">录制记录</Link>
          <Link to="/admin/quality" className="block p-2 hover:bg-gray-100">质检复核</Link>
        </nav>
      </aside>
      
      {/* 主内容区 */}
      <div className="flex-1 flex flex-col">
        <header className="h-16 bg-white border-b flex items-center px-6">
          <h1 className="text-xl font-bold">智能双录系统 - 管理后台</h1>
        </header>
        <main className="flex-1 overflow-auto p-6">
          <Outlet />
        </main>
      </div>
    </div>
  );
};
```

路由配置：
```typescript
// routes.tsx
<Route path="/admin" element={<AdminDashboard />}>
  <Route path="plugins" element={<PluginMarket />} />
  <Route path="flows" element={<FlowDesigner />} />
  <Route path="records" element={<RecordList />} />
  <Route path="quality" element={<QualityReview />} />
</Route>
```
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>管理后台布局创建完成，路由配置正确，编译通过</done>
</task>

<task type="auto">
  <name>Task 2: 实现插件市场页面</name>
  <files>
    admin-react/src/pages/admin/PluginMarket.tsx
    admin-react/src/types/plugin.ts
  </files>
  <action>
创建插件市场页面（使用 shadcn/ui 组件）：

```typescript
// PluginMarket.tsx
import { useEffect, useState } from 'react';
import axios from 'axios';
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table';
import { Switch } from '@/components/ui/switch';
import { Button } from '@/components/ui/button';

interface Plugin {
  id: string;
  name: string;
  version: string;
  enabled: boolean;
  description: string;
}

export const PluginMarket = () => {
  const [plugins, setPlugins] = useState<Plugin[]>([]);
  
  useEffect(() => {
    axios.get('/api/admin/plugins').then(res => setPlugins(res.data));
  }, []);
  
  const togglePlugin = async (id: string, enabled: boolean) => {
    await axios.post(`/api/admin/plugins/${id}/toggle`, { enabled });
    setPlugins(plugins.map(p => p.id === id ? { ...p, enabled } : p));
  };
  
  return (
    <div>
      <h1 className="text-2xl font-bold mb-4">插件市场</h1>
      <Table>
        <TableHeader>
          <TableRow>
            <TableHead>插件名称</TableHead>
            <TableHead>版本</TableHead>
            <TableHead>描述</TableHead>
            <TableHead>状态</TableHead>
            <TableHead>操作</TableHead>
          </TableRow>
        </TableHeader>
        <TableBody>
          {plugins.map(plugin => (
            <TableRow key={plugin.id}>
              <TableCell className="font-medium">{plugin.name}</TableCell>
              <TableCell>{plugin.version}</TableCell>
              <TableCell>{plugin.description}</TableCell>
              <TableCell>
                <Switch 
                  checked={plugin.enabled} 
                  onCheckedChange={enabled => togglePlugin(plugin.id, enabled)} 
                />
              </TableCell>
              <TableCell>
                <Button variant="outline" size="sm">配置</Button>
              </TableCell>
            </TableRow>
          ))}
        </TableBody>
      </Table>
    </div>
  );
};
```
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>插件市场页面创建完成，可展示和切换插件状态</done>
</task>

</tasks>

<verification>
- [ ] 管理后台布局正确（侧边栏、顶栏、内容区）
- [ ] 路由配置正确（/admin/plugins, /admin/flows, /admin/records, /admin/quality）
- [ ] 插件市场可展示插件列表
- [ ] 启用/停用开关正常工作
- [ ] 编译通过（npm run build）
</verification>

<success_criteria>
1. ✅ 管理后台主布局完成
2. ✅ 插件市场页面完成
3. ✅ 路由配置正确
4. ✅ 与后端 API 集成
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-client/08-01-SUMMARY.md`
</output>
